services:
  postgres:
    command:
      - "-c"
      - "tierdb.embedded_worker=on"
      - "-c"
      - "tierdb.worker_env=TIERDB_S3_ENDPOINT=http://rustfs:9000;TIERDB_CYCLE_INTERVAL_SECONDS=5;TIERDB_TIERING_LAG=0;TIERDB_CONSOLE_PORT=9090;TIERDB_LOAD_TOKEN=tierdb-example"
